#include<stdio.h>
int main() {
    int i;
    int arr[] = {1,2, 2,3,3, 3, 3, 4};
    int n = 8;
    int k = 2;

    int freq[100] = {0};


    for( i = 0; i < n; i++) {
        freq[arr[i]]++;
    }
    for (i=0; i<100; i++) {
        if (freq[i]>0) {
            printf("frequency of %d is: %d\n", i, freq[i]);
        }
    }
    return 0;

}
