#include<stdio.h>
int main() {
    int n,m ,i;
    printf("Enter the size: ");
    scanf("%d",&n);
    int a[n];
    printf("Enter the elements:");
    for (i=0;i<n;i++) {
        scanf("%d",&a[i]);
    }
    printf("Enter the size: ");
    scanf("%d",&m);
    int b[m];
    printf("Enter the elements:");
    for (i=0;i<n;i++) {
        scanf("%d",&b[i]);
    }
    int count =0;
    for (i=0;i<n;i++) {
        int found =0;
        for (int j=0;j<m;j++) {
            if (a[i] == b[j]) {
                found=1;
                break;
            }
        }
    if (found  == 0) {
        printf("%d\n",a[i]);
        count++;
    }
}
if (count == 0) {
    printf("-1");
}
    else
    {
        printf("count=%d",count);
    }
return 0;
