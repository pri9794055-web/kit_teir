#include<stdio.h>
int main() {
    int E[]={7,0,5,1,3};
    int l[]={1,2,1,3,4};
    int n=5;
    int current =0;
    int max = 0;
    for(int i=0;i<n;i++) {
        current=current+E[i]-l[i];
        if(max<current) {
            max=current;
        }
    }
    printf("%d",max);
    return 0;
}
